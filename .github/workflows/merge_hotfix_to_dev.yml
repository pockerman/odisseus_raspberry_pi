
name: merge-hotfix-branch
on:
  pull_request:
    branches: [master]
    types: [closed]
jobs:
  check-branch:
    name: check-hotfix-branch
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        shell: bash
        run: echo "Heloo...."
